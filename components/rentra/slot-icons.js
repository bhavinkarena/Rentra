import { Clock, Moon, Sun } from 'lucide-react';

/** One glyph per visit type, so a slot reads at a glance before its label does. */
export const SLOT_ICONS = { day: Sun, night: Moon, full_day: Clock };
